{
  "name": "wshot",
  "version": "1.1.1",
  "description": "",
  "main": "./dist/wshot.js",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && ./node_modules/.bin/babel src --out-dir dist",
    "start": "node wshot.js",
    "test": "standard src"
  },
  "author": "Joe Lanman",
  "license": "ISC",
  "dependencies": {
    "commander": "^2.15.1",
    "puppeteer": "^1.2.0"
  },
  "bin": {
    "wshot": "./dist/wshot.js"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "standard": "^11.0.1"
  }
}
